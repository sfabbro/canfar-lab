# LaTeX

- Load latex-debug for build failures; academic-writing + academic-humanizer + unslop for prose; overleaf-git when syncing Overleaf.
- Prefer the project's existing engine and class; do not switch engines or document classes unless asked.
- Minimal diff; keep author macros and citation style.
- After non-trivial .tex edits, run latexmk (or say why skipped) and fix the first real log error.
- Never invent bibliography entries.
