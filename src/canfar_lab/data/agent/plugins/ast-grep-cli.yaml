# ast-grep-cli — installs the `sg` binary (not an agent).
# Skill docs (optional): npx skills add ast-grep/agent-skill
id: ast-grep-cli
kind: tool
addon: true
tags:
- lean
- python
summary: Install ast-grep (sg) CLI binary for structural edits
homepage: https://github.com/ast-grep/ast-grep
agents:
- cursor
install:
  type: cli-tool
  tool: ast-grep
